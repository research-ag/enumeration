[package]
name = "enumeration"
version = "0.0.1"
description = "Array of keys with inverse lookup"
repository = "https://github.com/research-ag/enumeration"
keywords = [ "set", "bijective", "map", "principal", "username", "lookup", "key" ]
license = "Apache-2.0"

[dev-dependencies]
test = "1.0.1"

[dependencies]
base = "0.9.1"